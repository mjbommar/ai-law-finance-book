% ============================================================================
% BACK COVER - Matching front cover style with glass author boxes
% ============================================================================
% Uses same background, subtle network overlay, and glass-effect boxes
% for author bios. Designed for US Trade 6x9 format.
% ============================================================================

% Set full-bleed geometry for back cover
\newgeometry{margin=0pt}
\thispagestyle{empty}

\begin{tikzpicture}[remember picture, overlay]

% Position at page origin
\begin{scope}[shift={(current page.south west)}]

% Page dimensions
\def\pagewidth{6in}
\def\pageheight{9in}

% ============================================================================
% BACKGROUND - Same as front cover
% ============================================================================
\shade[top color=MidSlate, bottom color=DeepSlate]
    (0,0) rectangle (\pagewidth, \pageheight);

% ============================================================================
% SUBTLE TEXTURE - Reduced from front cover
% ============================================================================
\pgfmathsetseed{43}
\foreach \i in {1,...,200} {
    \pgfmathsetmacro{\px}{rnd * 6}
    \pgfmathsetmacro{\py}{rnd * 9}
    \pgfmathsetmacro{\psize}{0.2 + rnd * 0.5}
    \pgfmathsetmacro{\popacity}{0.01 + rnd * 0.02}
    \fill[white, opacity=\popacity] (\px in, \py in) circle (\psize pt);
}
\foreach \i in {1,...,150} {
    \pgfmathsetmacro{\px}{rnd * 6}
    \pgfmathsetmacro{\py}{rnd * 9}
    \pgfmathsetmacro{\psize}{0.15 + rnd * 0.35}
    \pgfmathsetmacro{\popacity}{0.02 + rnd * 0.03}
    \fill[black, opacity=\popacity] (\px in, \py in) circle (\psize pt);
}

% ============================================================================
% SUBTLE CIRCLES - Lighter than front cover
% ============================================================================
\fill[CircleDeep, opacity=0.08] (1.2in, 7.8in) circle (1.8in);
\fill[CircleMid, opacity=0.06] (4.8in, 8.0in) circle (1.4in);
\fill[CircleLight, opacity=0.05] (3.0in, 1.5in) circle (1.6in);
\fill[white, opacity=0.01] (5.0in, 3.0in) circle (1.2in);

% ============================================================================
% SUBTLE NETWORK NODES - Background decoration
% ============================================================================
\pgfmathsetseed{44}
\foreach \i in {1,...,25} {
    \pgfmathsetmacro{\nx}{0.5 + rnd * 5}
    \pgfmathsetmacro{\ny}{0.5 + rnd * 8}
    \pgfmathsetmacro{\nsize}{1.5 + rnd * 2}
    \fill[EdgeGlow, opacity=0.15] (\nx in, \ny in) circle (\nsize pt);
}

% A few connecting lines
\draw[EdgeGlow, opacity=0.08, line width=0.3pt] (1in, 7in) -- (2.5in, 6.5in);
\draw[EdgeGlow, opacity=0.08, line width=0.3pt] (4in, 7.5in) -- (5in, 6.8in);
\draw[EdgeGlow, opacity=0.08, line width=0.3pt] (1.5in, 2in) -- (3in, 2.5in);
\draw[EdgeGlow, opacity=0.08, line width=0.3pt] (4.5in, 1.5in) -- (5.2in, 2.2in);

% ============================================================================
% HEADER - "About the Authors"
% ============================================================================
\node[anchor=center] at (3in, 8.3in) {
    {\fontsize{14}{16}\selectfont\color{CopperLight}A\kern0.04em B\kern0.04em O\kern0.04em U\kern0.04em T\kern0.12em\kern0.12em T\kern0.04em H\kern0.04em E\kern0.12em\kern0.12em A\kern0.04em U\kern0.04em T\kern0.04em H\kern0.04em O\kern0.04em R\kern0.04em S}
};

% Decorative line under header
\draw[CopperGlow, opacity=0.4, line width=0.4pt] (1.5in, 8.1in) -- (4.5in, 8.1in);

% ============================================================================
% GLASS AUTHOR BOXES
% ============================================================================

% --- Michael J. Bommarito II ---
\begin{scope}[shift={(0.4in, 5.6in)}]
    % Glass box background
    \fill[white, opacity=0.06, rounded corners=4pt] (0, 0) rectangle (5.2in, 1.55in);
    % Subtle border
    \draw[white, opacity=0.15, line width=0.5pt, rounded corners=4pt] (0, 0) rectangle (5.2in, 1.55in);
    % Top highlight
    \draw[white, opacity=0.08, line width=0.3pt] (0.1in, 1.5in) -- (5.1in, 1.5in);

    % Name
    \node[anchor=north west] at (0.15in, 1.4in) {
        {\fontsize{11}{13}\selectfont\bfseries\color{TextPure}Michael J. Bommarito II}
    };
    % Bio text
    \node[anchor=north west, text width=4.9in, align=left] at (0.15in, 1.1in) {
        {\fontsize{8.5}{11}\selectfont\color{TextSoft}%
        CEO of 273 Ventures and Adjunct Professor of Law at Michigan State University. His research applies machine learning and NLP to legal practice. Co-founded LexPredict (acquired 2018) and led the research demonstrating GPT-4's ability to pass the Bar Exam. Editor of \textit{Legal Informatics} (Cambridge, 2021).}
    };
\end{scope}

% --- Daniel M. Katz ---
\begin{scope}[shift={(0.4in, 3.55in)}]
    % Glass box background
    \fill[white, opacity=0.06, rounded corners=4pt] (0, 0) rectangle (5.2in, 1.55in);
    % Subtle border
    \draw[white, opacity=0.15, line width=0.5pt, rounded corners=4pt] (0, 0) rectangle (5.2in, 1.55in);
    % Top highlight
    \draw[white, opacity=0.08, line width=0.3pt] (0.1in, 1.5in) -- (5.1in, 1.5in);

    % Name
    \node[anchor=north west] at (0.15in, 1.4in) {
        {\fontsize{11}{13}\selectfont\bfseries\color{TextPure}Daniel M. Katz}
    };
    % Bio text
    \node[anchor=north west, text width=4.9in, align=left] at (0.15in, 1.1in) {
        {\fontsize{8.5}{11}\selectfont\color{TextSoft}%
        Professor of Law at Illinois Tech Chicago-Kent, Director of The Law Lab, and Academic Director of the Bucerius Center for Legal Technology. Named by the \textit{Financial Times} as one of the top 20 legal market shapers of the past two decades. Co-founded 273 Ventures and LexPredict.}
    };
\end{scope}

% --- Jillian Bommarito ---
\begin{scope}[shift={(0.4in, 1.5in)}]
    % Glass box background
    \fill[white, opacity=0.06, rounded corners=4pt] (0, 0) rectangle (5.2in, 1.55in);
    % Subtle border
    \draw[white, opacity=0.15, line width=0.5pt, rounded corners=4pt] (0, 0) rectangle (5.2in, 1.55in);
    % Top highlight
    \draw[white, opacity=0.08, line width=0.3pt] (0.1in, 1.5in) -- (5.1in, 1.5in);

    % Name
    \node[anchor=north west] at (0.15in, 1.4in) {
        {\fontsize{11}{13}\selectfont\bfseries\color{TextPure}Jillian Bommarito}
    };
    % Bio text
    \node[anchor=north west, text width=4.9in, align=left] at (0.15in, 1.1in) {
        {\fontsize{8.5}{11}\selectfont\color{TextSoft}%
        Co-founder and Chief Risk Officer of 273 Ventures, leading compliance, privacy, and AI governance initiatives. A CPA with CIPP/US and CIPP/E certifications and one of the world's first certified AI auditors. Previously co-founded LexPredict.}
    };
\end{scope}

% ============================================================================
% BOTTOM - Book info and URL
% ============================================================================
\node[anchor=center] at (3in, 0.7in) {
    {\fontsize{8}{10}\selectfont\color{TextMuted}%
    Part of \textit{Artificial Intelligence for Law and Finance}}
};

\node[anchor=center] at (3in, 0.4in) {
    {\fontsize{7}{9}\selectfont\color{TextMuted}%
    github.com/mjbommar/ai-law-finance-book}
};

\end{scope}
\end{tikzpicture}

% Restore normal geometry
\restoregeometry
\clearpage
