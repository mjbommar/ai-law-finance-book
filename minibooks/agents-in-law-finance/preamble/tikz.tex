% ============================================================================
% TIKZ - Graphics and diagram setup
% ============================================================================

\usepackage{tikz}
\usetikzlibrary{
  positioning,
  arrows.meta,
  shapes.geometric,
  shapes.misc,
  calc,
  decorations.pathreplacing,
  decorations.pathmorphing,
  backgrounds,
  fit,
  shadows.blur,
  shadings,
  fadings,
  shadows
}

% Note: Do NOT override TikZ node fonts globally here.
% Figures use explicit font sizes (\tiny, \scriptsize, etc.) that must be preserved.
